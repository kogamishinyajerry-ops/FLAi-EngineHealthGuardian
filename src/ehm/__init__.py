"""EHM — COMAC Engine Health Guardian.

Top-level package. The four brains (data / knowledge / safety) plus the agent
are subpackages; ``core`` holds the shared kernel (Canonical Data Model, the
Evidence spine, telemetry, errors). See ``docs/architecture.md``.
"""

__version__ = "0.0.1"
